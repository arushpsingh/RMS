import React from "react";
import ShowResources from "./ShowResources";

const page = () => {
    return <ShowResources />;
};

export const metadata = {
    title: "Show Resource: Resource Manager",
};

export default page;
